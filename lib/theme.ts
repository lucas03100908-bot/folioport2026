/**
 * Single source of truth for the base colour.
 * Consumed by the body background (globals.css + layout inline style) and the
 * meta theme-color tag — so there is never a flash on first paint.
 */
export const BASE_HEX = "#000000";

/** Accent — active category, dividers, CTAs. Nothing else. */
export const ACCENT_HEX = "#FF4D1C";
