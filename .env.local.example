# Adobe Fonts (Typekit) web project id for the Fiona family.
# fonts.adobe.com → Web Projects → create a project with "Fiona" → copy the id
# from the embed snippet (https://use.typekit.net/<id>.css), then:
#   cp .env.local.example .env.local
NEXT_PUBLIC_TYPEKIT_ID=
